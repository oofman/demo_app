<h1>Editing Champion</h1>

<%= render 'form', champion: @champion %>

<%= link_to 'Delete', @champion, method: :delete, data: { confirm: 'Are you sure?' } %>
| <%= link_to 'Cancel', champions_path %>
